import React from 'react'

function AnimatedCounter({amount}: {amount:number}) {
  return (
    <div className='w-full'>
        {/* <CountUp decimal=',' prefix='$' duration={2.75} decimal={2} end={amount}/> */}
        </div>
  )
}

export default AnimatedCounter